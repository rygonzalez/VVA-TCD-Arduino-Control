//  // These require different syntax for arduino uno rev2 wifi, differ per board. Check:
//  // https://www.arduino.cc/reference/en/language/functions/external-interrupts/attachinterrupt/
#include "Limit_Switch.h"

//Limit_Switch::Limit_Switch(byte pin, void trigger) {
//  this->pin = pin;
//  init();
//}
//void Limit_Switch::init() {
//  digitalWrite(pin, HIGH);
//  state = 0;
//  attachInterrupt(digitalPinToInterrupt(pin), trigger, CHANGE);
}

//void Limit_Switch::trigger() {
//  if (digitalRead(pin) == HIGH) {
//    state = 0;
//  } else {
//    state = 1;
//  }
//}
